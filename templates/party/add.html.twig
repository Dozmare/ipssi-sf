{% extends 'base.html.twig' %}

{% block body %}
    {% if isOk %}
        <h1>Le jeux a bien été crée</h1>
    {% else %}
        {{ form_start(partyForm) }}
        {{ form_rest(partyForm) }}
        <button type="submit">Ajouter le jeux</button>
        {{ form_end(partyForm) }}
    {% endif %}
{% endblock %}
